# Cloudflare Pages redirects
# Root → English (server-side 301, no Loading flash)
/   /en/  301

# Sitemap access
/sitemap-index.xml   /sitemap-index.xml  200

# Never serve Astro build artifacts under /pages/ (soft-404 source)
/pages/*   /   301
